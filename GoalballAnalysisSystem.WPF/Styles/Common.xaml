<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--Colors-->
    <Color x:Key="GrayColor">#777777</Color>
    <Color x:Key="DarkGrayColor">#444444</Color>
    <Color x:Key="LightBlueColor">#D9E1E2</Color>
    <Color x:Key="BlueColor">#616675</Color>
    <Color x:Key="DarkBlueColor">#3c4740</Color>
    <Color x:Key="LightYellowColor">#F3F3ED</Color>
    <Color x:Key="WhiteColor">White</Color>
    
    <!--Brushes-->
    <SolidColorBrush x:Key="LightButtonBrush" Color="{StaticResource GrayColor}"/>
    <SolidColorBrush x:Key="DarkButtonBrush" Color="{StaticResource DarkGrayColor}"/>
    <SolidColorBrush x:Key="BlueButtonBrush" Color="{StaticResource BlueColor}"/>
    <SolidColorBrush x:Key="DarkBlueButtonBrush" Color="{StaticResource DarkBlueColor}"/>

    <SolidColorBrush x:Key="MainBackgroundBrush" Color="{StaticResource LightYellowColor}"/>
    <SolidColorBrush x:Key="FirstBackgroundBrush" Color="{StaticResource WhiteColor}"/>
    <SolidColorBrush x:Key="SecondBackgroundBrush" Color="{StaticResource LightBlueColor}"/>


    <!--Text Boxes-->
    <Style x:Key="TextBoxBase" TargetType="Control">
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Control">
                    <Border BorderThickness="1"
                            BorderBrush="{StaticResource FirstBackgroundBrush}"
                            CornerRadius="3">
                        <ScrollViewer x:Name="PART_ContentHost"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TextBox" BasedOn="{StaticResource TextBoxBase}"/>
    <Style TargetType="PasswordBox" BasedOn="{StaticResource TextBoxBase}"/>

    <!--Buttons-->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource DarkButtonBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="10 5"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border CornerRadius="3" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource LightButtonBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>